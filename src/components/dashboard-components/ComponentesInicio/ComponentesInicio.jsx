import React from "react";
import Slider from '../ComponentSlider';
import {
    Card,
    CardBody,
    CardTitle,
} from 'reactstrap';

const Feeds = () => {
    return(
        <React.Fragment>
            <Slider />
        </React.Fragment>
    );
}

export default Feeds;
